apiVersion: v1
kind: Service
metadata:
  name: mlops-serving-service
  namespace: mlops
spec:
  selector:
    app: mlops-serving
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: NodePort
